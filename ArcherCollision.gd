extends CollisionShape2D

var mouse_hovered_bool : bool = false
# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
	
#func _draw():
	#if(mouse_hovered):
		#draw_circle(self.position,500,Color(0,0,0,0.5))
#
#func mouse_hovered(mouse_hovered: bool) -> void:
	#mouse_hovered_bool = !mouse_hovered_bool
	#_on_draw()
#
#func _on_draw() -> void:
	#pass # Replace with function body.
